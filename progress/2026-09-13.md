# 2026-09-13

## Session: Cubware build guides on the Build It page

### Work completed
- Added `scripts/build-docs.py`, which renders the markdown guides in
  `Cubware/documentation/` into styled pages under `docs/` (site header,
  footer, breadcrumb, sticky on-page TOC). Relative repo links are rewritten
  to GitHub URLs; images are served from `assets/docs/`.
- Rendered two guides: `docs/vial-capper-decapper-build.html` and
  `docs/opentrons-pipette-setup.html`.
- Added a "Build guides" card grid to `build.html` linking both pages, and
  retitled the still-pending section to "Full CubXL + ASMI assembly".
- Appended `.guide-*` and `.doc-*` styles to `styles.css`.

### Issues found
- On phone widths the single-column doc layout used `1fr`, so the wide BOM
  tables pushed the prose past the viewport.

### Resolution
- Switched the collapsed column to `minmax(0, 1fr)` and gave `.doc-body`
  `min-width: 0`; tables scroll inside `.doc-table-wrap` instead.

### Verification
- Served with `python3 -m http.server` and checked both guide pages and the
  build page at desktop and 375px widths: no console errors, images load,
  no horizontal page scroll.

### Notes
- Re-run `python3 scripts/build-docs.py [path/to/Cubware]` whenever the
  Cubware docs change; the rendered HTML is committed, not generated at deploy.


## Session: Choose your build journey

- Replaced the ASMI-first introduction with a three-step instrument, platform,
  and guide finder. Supports multiple instruments, calibration-only browsing,
  editable choices, and reset using native controls without dependencies.
- Preserved the CAD viewer and existing guides; added a direct guide library.
- Added Cub, CubXL and CubXL+ calibration pages with source PDF downloads,
  platform-specific peg counts, print reference settings and source illustrations.
- Recommendations describe documented paths. Mixed ASMI/PANDA selections or
  platform mismatches show guide coverage gaps instead of claiming compatibility.
- Verified all 24 tool/platform combinations in Chromium; back/edit state,
  reset, keyboard selection, no-JavaScript browsing, local links and 375px
  layouts passed. No browser page errors. Inspected desktop and mobile screenshots.
- Preview: `python3 -m http.server 8765 --bind 127.0.0.1` in this repository.
- Canonical plan: `Ursa_Context/docs/plans/2026-09-13-build-guide-journey.md`.
- Product pages and complete system assembly remain future work. No deployment
  or hardware validation was performed.
